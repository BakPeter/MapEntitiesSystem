<Window x:Class="MapPresenterApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MapPresenterApplication"
        mc:Ignorable="d"
        Title="MapPresenterApp" Height="450" Width="800">
    <Grid x:Name="MainViewGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Image x:Name="MissionMapImage"
               Grid.Row="0"
               Grid.Column="0"
               Stretch="Fill"/>
        <Canvas x:Name="EntitiesShowerCanvas"
               Grid.Row="0"
               Grid.Column="0" />
    </Grid>
</Window>
